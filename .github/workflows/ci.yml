name: Test and Publish
on:
  push:
  pull_request:
    branches:
      # Branches from forks have the form 'user:branch-name' so we only run
      # this job on pull_request events for branches that look like fork
      # branches. Without this we would end up running this job twice for non
      # forked PRs, once for the push and then once for opening the PR.
    - '**:**'

jobs:
  # Build the package
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Install python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'

    - name: Install poetry
      uses: Gr1N/setup-poetry@v7

    - name: Build package
      run: poetry build

    - name: Upload built package
      uses: actions/upload-artifact@v3
      with:
        name: dist
        path: dist/
        retention-days: 1

  # Run pytest using built package
  # test:
  #   needs: build
  #   runs-on: ubuntu-latest

  #   steps:
  #   - name: Checkout repository
  #     uses: actions/checkout@v2

  #   - name: Install python
  #     uses: actions/setup-python@v2
  #     with:
  #       python-version: '3.9'
  #       cache: 'pip'
  #       cache-dependency-path: "poetry.lock"

  #   - name: Download built package
  #     uses: actions/download-artifact@v3
  #     with:
  #       name: dist
    
  #   - name: Install summit and pytest
  #     run: pip install summit-*.tar.gz pytest

    # - name: Run tests
    #   run: summit-tests

  # Publish to pypi on version change
  # This is based on https://github.com/coveooss/pypi-publish-with-poetry
  publish:
    # needs: test
    needs: build
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Install python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'

    - name: Download built package
      uses: actions/download-artifact@v3
      with:
        name: dist
        path: dist/

    - name: Install poetry
      uses: Gr1N/setup-poetry@v7

    - name: Install coveo-pypi-cli
      run: pip install coveo-pypi-cli

    - name: Determine the version for this release from the build
      id: current
      run: |
        BUILD_VER="$(ls dist/summit-*.tar.gz)"
        echo "Path: $BUILD_VER"
        if [[ $BUILD_VER =~ (summit-)([^,][0-9.]{4}) ]]; then
            echo "::set-output name=version::${BASH_REMATCH[2]}"
            echo "Version of build: ${BASH_REMATCH[2]}"
        else
            echo "No version found found"
        fi
        

    - name: Get latest published version
      id: published
      run: |
        PUB_VER="$(pypi current-version summit)"
        echo "::set-output name=version::$PUB_VER"
        echo "Latest published version: $PUB_VER"
    
    - run: |
        echo "$Current: {{ steps.outputs.currrent.version }}"
        echo "$Published {{ steps.outputs.published.version }}"

    - name: Publish to pypi if new version
      if: steps.outputs.currrent.version != steps.outputs.published.version
      run: |
        poetry config pypi-token.pypi ${{ secrets.PYPI_TOKEN }}
        if  [[ ${{ github.ref_name }} == "master"]]; then
          poetry publish 
        else
          poetry publish --dry-run
        fi

    - name: Tag repository
      shell: bash
      id: get-next-tag
      if: (steps.outputs.currrent.version != steps.outputs.published.version)
      run:
        TAG_NAME=${{ steps.outputs.currrent.version }}
        echo "::set-output name=tag-name::$TAG_NAME"
        echo "This release will be tagged as $TAG_NAME"
        git config user.name "github-actions"
        git config user.email "actions@users.noreply.github.com"
        git tag --annotate --message="Automated tagging system" $TAG_NAME ${{ github.sha }}

    - name: Push the tag
      if: (steps.outputs.currrent.version != steps.outputs.published.version)
      env:
        TAG_NAME: ${{ steps.get-next-tag.outputs.tag-name }}
      run: |
        if  [[ ${{ github.ref_name }} == "master"]]; then
          git push origin $TAG_NAME
        else
          echo "If this was the master branch, I would push this new tag named $TAG_NAME"
        fi
